# TOEC COGNITIVE FABRIC HARDWARE RC EXTRACTION RULES CONFIGURATION
# PLATFORM TARGET NODE: DESKTOP-9DLG3I0
# DESIGN FOUNDER & AUTHOR: AMR TORKY

# Enforce high-fidelity extraction scaling vectors across metal interconnect layers
set ::env(RCX_CORNER_COUNT) 1
set ::env(RCX_CC_MODEL) "sky130A_typical"

# Extraction layer sensitivity bounding configurations
set ::env(RCX_MAX_RESISTANCE_VAL) 12.5
set ::env(RCX_COUPLING_THRESHOLD) 0.15
set ::env(SPEF_EXTRACTOR) "openrcx"

echo "STATUS: extraction.cfg successfully written to local disk repository."
